# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Publish profiles (contain credentials)
*.PublishSettings

# Azure Functions local settings (contain secrets/API keys - NEVER commit)
azure-function/local.settings.json

# Firebase Functions env (contain secrets/API keys - NEVER commit)
functions/.env
functions/.env.*

# Google service account key files
*.json
!tsconfig*.json
!package*.json
!public/manifest.json
!public/staticwebapp.config.json
